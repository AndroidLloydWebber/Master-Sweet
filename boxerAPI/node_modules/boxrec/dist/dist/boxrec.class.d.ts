import { BoxrecEvent, BoxrecProfile, BoxrecRating, BoxrecSearch } from "./boxrec-pages/boxrec.constants";
import { BoxrecPageChampions } from "./boxrec-pages/champions/boxrec.page.champions";
export declare class Boxrec {
    private _cookieJar;
    login(username: string, password: string): Promise<void>;
    getBoxerById(boxrecBoxerId: number): Promise<BoxrecProfile>;
    /**
     * Returns individual BoxRec profiles
     * by using a generator, we're trying to prevent making too many calls to BoxRec
     * @param {string} firstName
     * @param {string} lastName
     * @param {string} active   default is false, which includes active and inactive
     */
    getBoxersByName(firstName: string, lastName: string, active?: boolean): AsyncIterableIterator<BoxrecProfile>;
    getChampions(): Promise<BoxrecPageChampions>;
    getRatings(qs: any): Promise<BoxrecRating[]>;
    getEventById(eventId: number): Promise<BoxrecEvent>;
    search(qs: any): Promise<BoxrecSearch[]>;
    private getSessionCookie();
    private checkIfLoggedIntoBoxRec();
}
